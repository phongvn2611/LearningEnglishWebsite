/// <reference types="react" />
import { MaterialUIMediaProps } from '../types';
export interface AudioCardProps extends MaterialUIMediaProps {
    mute?: boolean;
}
export declare const AudioCard: (props: AudioCardProps) => JSX.Element;
