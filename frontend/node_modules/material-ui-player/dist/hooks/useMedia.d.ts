import { FadeSettings } from '../types';
import { State } from '../state/types';
declare const useMedia: (initialState: State, fadeSettings?: FadeSettings | undefined) => {
    state: State;
    pause: (el: HTMLMediaElement) => void;
    stop: (el: HTMLMediaElement) => void;
    setCurrentTime: (el: HTMLMediaElement, value?: number | undefined) => void;
    load: (el: HTMLMediaElement, url: string) => void;
    play: (el: HTMLMediaElement) => Promise<void>;
    setProgress: (el: HTMLMediaElement, value: number) => Promise<void>;
    setSize: (el: HTMLVideoElement) => void;
};
export default useMedia;
